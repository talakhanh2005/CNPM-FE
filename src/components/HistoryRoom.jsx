const History = () => {
  return (
    <section
      aria-label="Lịch sử phòng"
      className="flex h-full w-full items-center justify-center rounded-[16px] bg-white"
    >
      <p className="text-[24px] text-[#222]">Trang lịch sử tạm thời</p>
    </section>
  );
};

export default History;