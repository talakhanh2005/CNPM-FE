const NewRoom = () => {
	return (
		<section
			aria-label="Tạo phòng"
			className="flex h-full w-full items-center justify-center rounded-[16px] bg-white"
		>
			<p className="text-[24px] text-[#222]">Trang tạo phòng tạm thời</p>
		</section>
	);
};

export default NewRoom;
